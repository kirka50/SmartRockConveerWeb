import React from 'react';

import { Chart as Chartjs } from 'chart.js/auto'

const FlowImg = ({...props}) => {
    return (
        <img {...props}>

        </img>
    );
};

export default FlowImg;